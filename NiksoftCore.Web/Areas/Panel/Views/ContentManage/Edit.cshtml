
@model NiksoftCore.ViewModel.ContentRequest
@{
    ViewData["Title"] = "ویرایش محتوا";
    Layout = "~/Areas/Panel/Views/Shared/_FaLayout.cshtml";
    var Messages = (List<NiksoftCore.ViewModel.NikMessage>)ViewBag.Messages;
}
<div class="text-right">
    <div class="btn-group">
        <a href="/Panel/ContentManage" class="btn btn-outline-info btn-sm">
            <i class="fas fa-arrow-left"></i>
        </a>
    </div>
</div>
<br />
@Html.ValidationSummary()
<form action="/Panel/ContentManage/Edit" method="post" enctype="multipart/form-data">
    <div class="registerdiv">
        <div class="row">
            <div class="col-sm-6 offset-sm-3">
                <div class="form-group">
                    <label for="txtTitle" class="control-label">عنوان</label>
                    @Html.TextBoxFor(m => m.Title, new { placeholder = "عنوان", @class = "form-control", @id = "txtTitle" })
                </div>
                <div class="form-group">
                    <label for="ParentId" class="control-label">دسته بندی</label>
                    <select class="form-control m-b" asp-for="CategoryId" asp-items="ViewBag.Categories">
                        <option selected="selected" value="0">انتخاب کنید</option>
                    </select>
                    <span asp-validation-for="CategoryId" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label for="txtKeyValue" class="control-label">کلید(انگلیسی)</label>
                    @Html.TextBoxFor(m => m.KeyValue, new { placeholder = "key", @class = "form-control", @id = "txtKeyValue" })
                </div>
                <div class="form-group">
                    <label for="txtIcon" class="control-label">آیکن</label>
                    @Html.TextBoxFor(m => m.Icon, new { placeholder = "آیکن", @class = "form-control", @id = "txtIcon" })
                </div>
                <div class="form-group">
                    <label for="ImageFile" class="btn btn-warning">
                        انتخاب فایل
                        <input asp-for="ImageFile" type="file" class="d-none" />
                    </label>
                </div>
                <div class="form-group">
                    <label for="txtHeader" class="control-label">لید</label>
                    @Html.TextAreaFor(m => m.Header, new { placeholder = "توضیحات", @class = "form-control", @id = "txtHeader", rows = "3" })
                </div>
                <div class="form-group">
                    <label for="txtBodyText" class="control-label">متن اصلی</label>
                    @Html.TextAreaFor(m => m.BodyText, new { @class = "form-control text-editor", @id = "txtBodyText", rows = "8" })
                </div>
                <div class="form-group">
                    <label for="txtFooter" class="control-label">متن پایین</label>
                    @Html.TextAreaFor(m => m.Footer, new { placeholder = "توضیحات", @class = "form-control", @id = "txtFooter", rows = "3" })
                </div>
                @Html.HiddenFor(m => m.Id)
                <div class="form-group text-center">
                    <button type="submit" class="btn btn-success">ذخیره</button>
                </div>
            </div>
        </div>
    </div>
</form>


<script>

    $(document).ready(function () {

        @if (Messages != null)
        {
            @foreach (var item in Messages)
            {
                if (item.Type == NiksoftCore.ViewModel.MessageType.Error)
                {
                    <text>
                    showMessage({
                        text: '@item.Message',
                        type: 'error'
                    });
                    </text>
                }
                else
                {
                    <text>
                    showMessage({
                        text: '@item.Message',
                        type: 'success'
                    });
                    </text>
                }
            }
        }


    });
</script>